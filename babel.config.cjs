module.exports = {
  presets: [
    '@babel/preset-env',   // For transforming ES module code
    '@babel/preset-react',  // For JSX/React support
  ]
  // Ensure babel can handle .ts/.tsx if you're using those as well
};
